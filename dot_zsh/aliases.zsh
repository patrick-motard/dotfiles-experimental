# ~/.zsh/.zshrc
alias sz="source $ZDOTDIR/.zshrc"
alias csz="cat $ZDOTDIR/.zshrc"

# ~/.zshenv
alias szenv="source ~/.zshenv"
alias czenv="source ~/.zshenv"

# Directories
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."

# Git
# nice shorthand,plus it gets rid of annoyingly launching ghostscript on accident.
alias gs="git status"

# Programs
alias moi="chezmoi"
